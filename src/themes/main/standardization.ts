import palette from "./palette";

const button_borders = {
  primary: `1px solid ${palette.core.secondary}`,
  danger: `2px solid ${palette.core.danger}`,
};

const standardization = { button_borders };

export default standardization;
