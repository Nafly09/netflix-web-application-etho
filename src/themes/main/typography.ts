const fonts = {
  primary: "Roboto, sans-serif",
  secondary: "Helvetica Neue, sans-serif",
};

const sizes = {
  categoryTitle: "24px",
  Headers: "54px",
  paragraphs: "18px",
  placeholders: "16px",
};

const weights = {
  primary: 500,
  focus: "bold",
};

const typography = {
  fonts,
  sizes,
  weights,
};

export default typography;
